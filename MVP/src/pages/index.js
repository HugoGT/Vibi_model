import * as React from "react";
import { Layout } from "../components/layout";
import { Header } from "../components/header";

const links = [{ url: "#servicios", name: "Servicios" }];

export default function index() {
  return <Layout title="Vibi"></Layout>;
}
